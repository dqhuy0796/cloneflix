function Footer({ children }) {
    return (
        <footer>
            <div>Privacy</div>
            <nav>Condition</nav>
            <div>About</div>
        </footer>
    );
}

export default Footer;
